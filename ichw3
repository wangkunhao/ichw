把程序和数据存放在主存里，在CPU执行已在缓存中的部分指令和数据时，利用这个时间间隙，把CPU下一阶段需要的一部分程序和数据预先搬进缓存，这样就可以大大提高
CPU的工作效率.CPU需要存取指令或数据时首先到高速缓存里去找，如果所需要的东西当时正在缓存里，访问立即完成，CPU不必访问主存就可以往下继续工作。如果所需要
的东西当时不在缓存里，CPU就转而对主存储器进行读取，与此同时，负责存储管理的专门硬件把这次被访问的信息也由主存搬到高速缓存中。
